with entry{
    # Example: Counting down from 5 to 1 using a while loop

    count = 5;

    while count > 0{
        print(f"Countdown: {count}");
        count -= 1;
        }  # Decrement the count

    print("Blastoff!");

}