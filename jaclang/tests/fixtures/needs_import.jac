"""Test of jac importing python."""
import:py pyclass;
import:py from pyclass, PyClass;

with entry {
    # custom_shape = PyClass(5.0, 2.0);
    # print(f"Area of custom shape with radius {custom_shape.radius} and multiplier {custom_shape.multiplier}: {custom_shape.area()}");
    # my_print(circle);
    print("something");
}

