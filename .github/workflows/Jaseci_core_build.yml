# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: Jaseci_core - Build and Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.10
      uses: actions/setup-python@v2
      with:
        python-version: "3.10"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
         pip install build
         
#     - name: Install Jaseci
#       run: |
#         source jaseci_core/install.sh
#     - name: check Jaseci
#       run: |
#         jsctl
#     - name: Test Jaseci Setup
#       run: |
#         source jaseci_core/test.sh
    - name: Check File Dir before Build
      run: |
        ls -l jaseci_core/
        
    - name: Build package
      run: python -m build jaseci_core/
      
    - name: Check File Dir after Build
      run: |
        ls -l jaseci_core/
    
    - name: Publish package
      uses: pypa/gh-action-pypi-publish@27b31702a0e7fc50959f5ad993c78deac1bdfc29
      with:
        user: __token__
        password: pypi-AgEIcHlwaS5vcmcCJDU4MGViNDVhLTgyMjMtNDdhNi04NTAzLTUyY2FkZGUwYmFmMQACJXsicGVybWlzc2lvbnMiOiAidXNlciIsICJ2ZXJzaW9uIjogMX0AAAYgGfohyf1iNbpnvd7V126lOcbScqMeYehIzQ6CCWcvEyw
